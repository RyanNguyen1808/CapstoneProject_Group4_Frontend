name: Angular CI for PROD

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Inject prod environment.ts
        run: |
          echo "export const environment = {" > src/app/environments/environment.ts
          echo "  production: true," >> src/app/environments/environment.ts
          echo "  userPoolId: '${{ secrets.PROD_USER_POOL_ID }}'," >> src/app/environments/environment.ts
          echo "  userPoolClientId: '${{ secrets.PROD_USER_POOL_APP_ID }}'" >> src/app/environments/environment.ts
          echo "};" >> src/app/environments/environment.ts

      - name: Install dependencies
        run: npm ci

      - name: Build Angular app
        run: npm run build -- --configuration production

      - name: Archive build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: angular-dist
          path: dist/
      
  deploy:
    needs: build  # <- THIS makes deploy wait for build
    uses: ./.github/workflows/prod-cd.yaml
    secrets:
      AWS_ACCESS_KEY_ID_NTU: ${{ secrets.AWS_ACCESS_KEY_ID_NTU }}
      AWS_SECRET_ACCESS_KEY_NTU: ${{ secrets.AWS_SECRET_ACCESS_KEY_NTU }}
      AWS_REGION_NTU: ${{ secrets.AWS_REGION_NTU }}
      PROD_S3_BUCKET_NAME: ${{ secrets.PROD_S3_BUCKET_NAME }}
      PROD_CLOUDFRONT_DISTRIBUTION_ID: ${{ secrets.PROD_CLOUDFRONT_DISTRIBUTION_ID }}